language: cpp
script: cmake

matrix:
    include:
        # OSX
        - os: osx
        # Linux
        - os: linux
          addons:
            apt:
              packages:
                - util-linux
                - python
                - cmake

script:
    - cmake --version
    - python --version
    - export PATH="$PREFIX/bin:$PATH"
    - python setup.py install --user && python ./test/test.py
